apiVersion: v1
kind: Service
metadata:
  name: k8s-opt-svc
  labels:
    app: k8s-opt
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: k8s-opt
