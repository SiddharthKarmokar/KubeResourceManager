apiVersion: v1
kind: ConfigMap
metadata:
  name: k8s-opt-config
  labels:
    app: k8s-opt
data:
  SERVER_PORT: "8080"
  RECOMMENDATION_CPU_SAFETY_BUFFER: "1.5"
  RECOMMENDATION_MEMORY_SAFETY_BUFFER: "1.3"
  RECOMMENDATION_MIN_CPU_MILLICORES: "100"
  RECOMMENDATION_MIN_MEMORY_MB: "128"
